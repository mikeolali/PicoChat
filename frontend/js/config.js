// ── CONFIG ─────────────────────────────────────────────────────────────────
// Change this URL once your backend is deployed on Render/Railway
const CONFIG = {
  API_URL: window.location.origin, // En prod, remplace par ton URL Render ex: "https://picochat.onrender.com"
  SOCKET_URL: window.location.origin
};
